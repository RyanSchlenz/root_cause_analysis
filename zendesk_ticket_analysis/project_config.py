# project_config.py

config = {
    'scripts': [
        'clean_data.py',
        'filter_groups.py',
        'filter_subjects.py',
        'aggregate.py'
    ],
    'csv_files': [
        'cleaned_data.csv',
        'filtered_groups.csv',
        'filtered_subjects.csv',
        'aggregated_data.csv'  # Add the output CSV file here if needed
    ]
}
